# Frontend Notes

## 目的

- React + Vite で WOL ダッシュボードを段階的に実装し、実務フローを学ぶ。
- 「要件整理 → 設計 → 実装 → 検証」の順で進める習慣を身につける。

## 変更内容

- 2026-02-20: 既存フロント実装をいったん初期化し、学習優先の段階実装へ方針変更。
- 2026-02-20: `health/targets/wol` に加えて `status` API 連携を追加（ターゲット単位で状態確認）。
- 2026-02-20: `logs` API 連携を追加（表示件数切替 + 最新ログ一覧表示）。

## 運用時の注意点

- 1ステップごとに動作確認を行い、問題があれば次へ進む前に修正する。
- API I/F 変更時はフロントとバックエンドを同時更新する。
- 配布対象は `frontend/dist/`。成果物はGit管理しない。
- コマンド運用: 読み取り系（例: `ls` `rg` `cat` `sed` `git status`）はCodex側で実行可。
- コマンド運用: 実行系（例: `npm` `python` `pytest` `uvicorn`）は学習者が実行する。

## 次に作るもの（予定）

- Status/Logs のエラー表示改善（ターゲット単位の見せ分け）
- `components` の役割整理（表示責務と状態管理責務の分離）
- 画面レイアウト調整（可読性向上）

## 学習ガイド

- 学習順は `docs/frontend/learning-path.md` を参照。
