# Frontend Notes

## 目的

- React + Vite 側の画面仕様、状態管理、API 連携方針を整理する。
- 開発時と本番ビルド時の挙動差分を明文化する。

## 変更内容

- 2026-02-11: フロントエンド向けドキュメントの初期ページを作成。
- 今後は画面一覧、コンポーネント方針、API クライアント設計を追記する。

## 運用時の注意点

- API I/F 変更時は `src/api/` 実装と本ドキュメントを同時更新する。
- 新規画面を追加したら、ルーティングと依存 API を記録する。
- `npm run build` 前提の配布手順変更があれば必ず反映する。

## 今後の追記候補

- 画面一覧と主要ユースケース
- コンポーネント分割ルール
- Vite proxy 設定メモ
