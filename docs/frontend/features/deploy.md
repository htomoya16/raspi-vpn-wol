# Deploy Feature

## 目的

- フロントエンドのビルド成果物を、運用環境へ安全に反映する手順を明確化する。

## 変更内容

- 本番配布は `npm run build` で生成される `frontend/dist/` を利用。
- CI では `lint` / `typecheck` / `test` / `build` を実行し、配布前に品質を担保。

## 運用時の注意点

- `dist/` は成果物のため、原則Git管理しない。
- ラズパイ配布はビルド済み静的ファイルを置き換える運用を基本とする。
